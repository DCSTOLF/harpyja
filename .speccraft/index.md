# Harpyja

A read-only, offline MCP server that turns a natural-language query into exact `file:line` citations across large/legacy/air-gapped codebases.

## Stack

- Python 3.12+
- FastMCP (MCP server; stdio + streamable HTTP)
- Tree-sitter (symbol layer: Go, Rust, Python, JS/TS, C#, Java, C/C++) + ripgrep fallback
- Microsoft FastContext (Tier 1 "Scout", pinned dependency)
- DSPy `dspy.RLM` over Deno/Pyodide WASM sandbox (Tier 2 "Deep")
- Local OpenAI-compatible model endpoint: llama.cpp (`llama-server`) or Ollama (4B-class quantized, 8 GB GPU profile)
- `uv` for env/deps

## Architecture in one paragraph

A FastMCP server exposes three tools (`locate` / `read` / `index`) and holds no business logic. The Orchestrator classifies each query, then runs the cheapest tier that can answer: Tier 0 (deterministic tree-sitter symbols + ripgrep), Tier 1 (Scout, a FastContext adapter), and Tier 2 (Deep, a `dspy.RLM` over bounded read-only host tools). A Verification Gate reads cited lines back and escalates on failure; a Citation Formatter merges, ranks, and clamps results. The Model Gateway is the single outbound abstraction, pointed only at localhost — the one place the air-gap guarantee is enforced. Every layer has a fallback so retrieval never goes blind.

## Hard rules (see guardrails.md)

- No network egress at runtime — model/search/parsing stay local; only the Model Gateway calls out, and only to localhost.
- Read-only — Harpyja returns citations and never edits, writes, or generates code in the target repo.
- Always degrade gracefully — no parser → ripgrep; no model → Tier 0; escalation fails → best-effort Tier 1 with a confidence flag.

## Where to look

- MCP server / transports: `harpyja/server/`
- Routing, classifier, verification gate, formatter: `harpyja/orchestrator/`
- File walker / manifest / hashing: `harpyja/index/`
- Tree-sitter + ripgrep engines (`CodeSpan`): `harpyja/symbols/`
- Tier 1 Scout (FastContext adapter): `harpyja/scout/`
- Tier 2 Deep (`dspy.RLM` + host tools): `harpyja/deep/`
- Model Gateway (llama.cpp / Ollama): `harpyja/gateway/`
- Settings / profiles: `harpyja/config/`
- CLI entrypoints (serve/index/locate/read): `harpyja/cli.py`

## Active spec

none

## Recent decisions (last 3)

- 2026-06-29 — **Scout path-suffix recovery shipped — rescue an out-of-repo hallucinated citation by its unique, manifest-keyed in-repo suffix, composing with (never bypassing) the 0011 drop + honesty floor** (specs/0012-path-prefix/): the spec-0010/0011 Q8 re-measurement exposed a `scout-empty` tail — **7/12** cases died because the model emitted **out-of-repo absolute paths** fabricated from the repo slug (e.g. `/pallets/flask/src/flask/blueprints.py`), which 0011 correctly **drops** (10 dropped in the N=12 auto run). Fix = **deterministic, model-independent** recovery in `normalize.py::_recover_suffix`: map such a citation to a real in-repo file by its **longest unique** path suffix against the repo's indexed manifest `file_set`, keeping a recovery **only** on **exactly one** match, with three guards against wrong-but-existing — ambiguous→drop, `MIN_TAIL_SEGMENTS=2` specificity floor, and a **manifest-keyed leading-segment guard** (the matched tail's head must be a known top-level entry; `src/flask/blueprints.py` kept, `flask/blueprints.py` dropped). Recovery **composes with, never bypasses** 0011: a recovered path re-enters repo-confine + `is_file` (+ clamp); `file_set` absent/empty ⇒ no recovery (graceful degrade). A recovered **file-level** citation stays `is_file_level` (None lines) so it **inherits** the 0011 `gate-skipped:no-line-range` floor and never reads as verified/high-confidence (AC3f, proven end-to-end). **Blast radius by category, producer→sink:** `normalize_spans_with_tally` 2-tuple→4-tuple + non-breaking `recovered_paths_out` out-param (deliberate over a 5-tuple — recorded smell); `ScoutTally.recovered_{spanned,filelevel}` + `recovered_filelevel_paths`; `ScoutEngine(file_set=…)`; `build_scout_engine` loads it via `read_manifest`; schema `0011/1`→`0012/1` with two additive last-with-default `fc_citation_recovered_*` fields summed in **both** `eval/runner.py` and `eval/swebench_eval.py` (the sibling-driver enumeration a reviewer flagged). **The `scout_model` Q8 default flip was NOT made** — deferred follow-up (rl favored 5/12 vs 2/12 gate-fire; variance-gated recommend→flip + missing-model degrade story required first). Verified TDD-complete: **678 unit pass**, ruff clean; integration **5 passed / 1 skipped** (AC5 env-gated). **AC5 live** (`run_q8rl_recovery_n12.json`, mode=fast, Q8 `fastcontext-4b-rl`, recovery ON vs the committed auto baseline): `scout_empty` **7→4** (3 of 7 dead cases rescued), `dropped` **10→0**, `recovered_spanned=10`, `recovered_filelevel=0` (every recovery was SPANNED — the model emitted out-of-repo paths *with* line numbers — so `recovered_filelevel_paths` is honestly empty; the riskiest un-gated category did not occur); N=12 < n_floor ⇒ `indicative_only`. Deviation: AC5 ran **fast not auto** — on the 16 GB dev host auto OOMs (jetsam; swap 18/19.5 GB) co-loading the 5 GB Q8 Scout + Deep + Deno/Pyodide, and Deep **crashes** via dspy `AdapterParseError` on the qwen model's malformed JSON (sphinx-9698); recovery is Scout-side so fast measures it in full. Open leads: Q8 default-flip follow-up; **gate false-escalation** of a correct Scout answer (requests-1766); **Deep `AdapterParseError`** robustness (should degrade to typed `DeepUnavailable`, not crash); two upstream FastContext bug reports (format_citations crash; recommended-Q4 quality); Wave-2.1 substring/fuzzy.
- 2026-06-28 — **Scout citation-shape robustness (seam (a), `citation=False`) + harness degrade visibility shipped — line-less `CodeSpan`, the corrected fix locus, safe + observable degradation** (specs/0011-citation-shape/): fixes the spec-0010 finding that Scout was **systematically dead on real SWE-bench** (12/12 `scout-degraded:backend-error`; bare Tier-0 16.7%) AND the enabling defect that the degrade was **invisible in aggregate metrics** — one spec, because the visibility gap is *why* the bug hid. **RCA corrected by a live spike (T0):** FC's `format_citations` runs **inside** `agent.run(citation=True)` and `TypeError`s when the model emits no parseable `<final_answer>` block; Harpyja **never receives FC citation *objects***, so the "normalize dict-vs-string objects" framing was a false premise. **Fix = seam (a):** Scout invokes FC **`citation=False`** (bypassing the crashing formatter — no catch-as-control-flow) and `parse_final_answer` parses the raw `<final_answer>` **text per line, anchored** to the spike-pinned grammar `<no-space-path>[:start[-end]] [(explanation)]`: `path:start` → spanned `CodeSpan`, bare/malformed-line path → **file-level** `CodeSpan` (`None` lines, no fabricated range; `_looks_like_path` + anchoring keep prose filenames out, AC22). FastContext is **NOT patched**; seam (c) contingency not needed. **Honest-precision representation:** `CodeSpan.start_line/end_line` → `int | None` (`None ⇒ file-level`) + `CodeSpan.is_file_level` as the single predicate every consumer branches on; half-`None` rejected at parse/normalize (AC23). **Blast radius closed by category** (`grep -rn 'start_line\|end_line'`, each consumer its own RED→GREEN, producer→…→metrics): `normalize_spans` (file-level branch + `normalize_spans_with_tally` dropped-count/log; Deep lined path byte-identical), `format.py` (file-level survives un-merged, None-safe rank), `gate.py` (`GateOutcome.skipped_reason="no-line-range"` detected before read-back), `locate.py` (stable `gate-skipped:no-line-range`, escalate-or-carry, distinct flag, auto+fast), `metrics.py` (`span_hit_kind` path-only credit before arithmetic in BOTH oracles — `span_hit_secondary` was an un-enumerated miss caught by the grep). **Deliverable 2:** schema `0010/1`→`0011/1`, 8 additive fields (last-with-defaults): `scout_degrade_count`, `scout_degrade_rate` (null-with-count on zero denom), `degraded_dominated`, composable `reliability_notes`, `fc_citation_{spanned,filelevel,dropped}_count`, run-metadata `degraded_dominated_threshold` (new eval-only `EvalConfig.degraded_dominated_threshold=0.5`, field-disjoint from `Settings`). Shape tally rides a **Scout-result side-channel** (`ScoutEngine.last_tally`/`ScoutTally`), read only by `eval/runner.py` (reset+read per case); orchestrator `list[CodeSpan]` seam unchanged. `compose_reliability_notes` shared by runner+swebench (R2); `is_file_level` shared predicate (R1). Verified TDD-complete: **656 unit pass** (+45), ruff clean; **7 live integration** (5 Scout + 2 SWE-bench, real FastContext-4B + Deep + Ollama). **AC20 proven live:** the exact 12/12-broken flask case returns with **zero backend-error** (22.5s). Deviations: T0 spike on a small temp repo (4B model returns empty on the full flask tree; seam/grammar repo-independent); AC21 N=12 re-run on the legacy fixture (full N=12 flask sweep is the compute-bound opt-in). Open: **production OQ2 calibration** (`verify_threshold`/`top_n`) this UNBLOCKS — now measurable since Scout fires on real data; `degraded_dominated_threshold` revisit once a real degrade distribution exists; Wave-2.1 substring/fuzzy.
- 2026-06-28 — **SWE-bench Verified eval dataset + per-case-repo driver shipped — real-data instrument, measurement-only, recommend-only, live-validated** (specs/0010-swebench-eval-dataset/): adds `harpyja/eval/swebench_eval.py` — a **standalone-localization** adapter (query the pre-fix repo at `base_commit`, score citations vs patch-derived pre-image hunk spans; **no Docker/patch/test-exec**) + a **per-case-repo driver** `run_swebench` that, since SWE-bench is one worktree per case (the 0009-6a harness was single-repo), builds its **own** `LocateStack` per case and pools outcomes into the **unchanged** metrics/recommend layers + an **additively-extended** report (`SCHEMA_VERSION` → `0010/1`, new fields appended-last-with-defaults so BOTH report shapes validate — the single anti-drift source). **D-class:** classify by patch shape (single-file ∧ span ≤ `POINT_SPAN_MAX_LINES=25` ⇒ point) — on the real N=50 sample **38 point / 12 broad**, a usable point subset so OQ2 is calibratable. **D-route (resolves review B1) — a RECORDED evaluation intervention:** production routes off `classify_query(query)` prose (uncorrelated with patch shape), so the driver injects `case.classification` through the existing `LocateStack.classifier` seam to force the gate to fire; the production label is captured BEFORE the override, both labels + an aggregate `classifier_agreement_rate` are recorded, `production_gate_ran` (SUT-observed) is kept distinct from the harness Scout-probe `gate_triggered`, and below `AGREEMENT_FLOOR=0.5` the OQ2 result is flagged `oq2_low_confidence`/`deltas-only`, never a calibration. **B2 fix:** `base_commit` lives only in raw JSONL (`provision` reads it via `_read_jsonl`); `load_dataset` ignores it. `mode=fast` seam threaded into `run_case` (Scout-terminal, gate informational). Durable report metadata: protocol id, new-file excluded + malformed-skipped counts, span-inflation tolerance, contamination caveat, dataset provenance (HF id/split/revision, raw sha256, sample ids). convert/provision are dev-time (may reach network) — scoped OUT of the runtime air-gap; run/sweep offline (live test asserts zero non-loopback egress). **No `Settings` default flipped (B1).** Verified: **611 unit pass** (ruff clean); **4 live integration in 185s** (FastContext + Deep `qwen2.5-coder:3b` + Deno + rg, incl. a real HF `convert` smoke); real `convert` → 50-case committed fixture; real `provision`+`run` on cloned **flask + requests** (2/2 resolved, span_hit_primary 0.5). **Real-data finding (corrected):** both point cases carry `scout-degraded:backend-error` — FastContext's own `format_citations` crashes on real-query output (spec-0007 AC10) → Tier-0 degrade, so Tier-1/gate were **upstream-starved** (NOT "gate didn't fire"/cheap-tier-works); 0.5 is the Tier-0 degrade-floor. **Implication:** Scout is non-functional on real SWE-bench until FastContext `format_citations` is hardened, so OQ2 can't be calibrated from this dataset regardless of N. Full OQ2 sweep over all 12 cloned repos is the compute-bound opt-in (`make swebench-full`→`swebench-sweep`). Open: harden FastContext Scout robustness on real data (new lead) → then large live OQ2 calibration → possible default flip; Wave-2.1 substring/fuzzy.
