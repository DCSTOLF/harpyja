# Harpyja

A read-only, offline MCP server that turns a natural-language query into exact `file:line` citations across large/legacy/air-gapped codebases.

## Stack

- Python 3.12+
- FastMCP (MCP server; stdio + streamable HTTP)
- Tree-sitter (symbol layer: Go, Rust, Python, JS/TS, C#, Java, C/C++) + ripgrep fallback
- Microsoft FastContext (Tier 1 "Scout", pinned dependency)
- DSPy `dspy.RLM` over Deno/Pyodide WASM sandbox (Tier 2 "Deep")
- Local OpenAI-compatible model endpoint: llama.cpp (`llama-server`) or Ollama (4B-class quantized, 8 GB GPU profile — ⚠️ **8 GB / Q4 floor NOT validated**: the recommended Q4_K_M Scout model is non-functional on real repos; the working config is Q8 (~2× memory, OOMs auto on 16 GB) — re-characterize the floor, see ARCHITECTURE.md "Footprint")
- `uv` for env/deps

## Architecture in one paragraph

A FastMCP server exposes three tools (`locate` / `read` / `index`) and holds no business logic. The Orchestrator classifies each query, then runs the cheapest tier that can answer: Tier 0 (deterministic tree-sitter symbols + ripgrep), Tier 1 (Scout, a FastContext adapter), and Tier 2 (Deep, a `dspy.RLM` over bounded read-only host tools). A Verification Gate reads cited lines back and escalates on failure; a Citation Formatter merges, ranks, and clamps results. The Model Gateway is the single outbound abstraction, pointed only at localhost — the one place the air-gap guarantee is enforced. Every layer has a fallback so retrieval never goes blind.

## Hard rules (see guardrails.md)

- No network egress at runtime — model/search/parsing stay local; only the Model Gateway calls out, and only to localhost.
- Read-only — Harpyja returns citations and never edits, writes, or generates code in the target repo.
- Always degrade gracefully — no parser → ripgrep; no model → Tier 0; escalation fails → best-effort Tier 1 with a confidence flag.

## Where to look

- MCP server / transports: `harpyja/server/`
- Routing, classifier, verification gate, formatter: `harpyja/orchestrator/`
- File walker / manifest / hashing: `harpyja/index/`
- Tree-sitter + ripgrep engines (`CodeSpan`): `harpyja/symbols/`
- Tier 1 Scout (FastContext adapter): `harpyja/scout/`
- Tier 2 Deep (`dspy.RLM` + host tools): `harpyja/deep/`
- Model Gateway (llama.cpp / Ollama): `harpyja/gateway/`
- Settings / profiles: `harpyja/config/`
- CLI entrypoints (serve/index/locate/read): `harpyja/cli.py`

## Active spec

none

## Recent decisions (last 3)

- 2026-07-04 — **Spec 0020 (OQ2 — the operator sweep) shipped the sequential G0→G3 operator protocol + `0020/1` gate-ledger; the live run produced a typed DEFERRED null** (specs/0020-oq2/): a **measurement/eval** spec — SUT **FROZEN**, all code in `harpyja/eval/`, additive. **777 → 820 unit pass** (+43), ruff clean. **Shipped:** `oq2_classify.py` (pure `classify_g3_outcome` projection **above** the byte-frozen `recommend_oq2` — precedence `DEGRADED_DOMINATED > GATE_CONFOUNDED > NOT_SEPARABLE > RECOMMENDATION`, no-survivor `S = incumbent_validated is False AND advantage_exceeds_variance is False` computed only when not gate-confounded, `indicative_only` a RECOMMENDATION-only sub-flag); `oq2_ledger.py` (**new** pinned `LEDGER_SCHEMA_VERSION = "0020/1"`, distinct from report `0014/1`, reuses `atomic_write_json`); `oq2_protocol.py` (G0→G1→G2→G3 stop-and-report driver, **close-vs-hold by cause** — env/OOM → `BLOCKED` hold, completed-then-degrade/false-reject → `STOP:SMOKE` close); `oq2_live.py` + `oq2` CLI + skip-not-fail integration test. Review's P1 (NOT_SEPARABLE discriminator reachable on the frozen `Recommendation`) + P2 (byte-frozen behavior snapshot) both locked. **Operator outcome — typed DEFERRED:** G0 PASS; G1 (astropy-12907) PASS but `tier1_correct=False` (vacuous sub-check-c); **G2 DEFERRED** — `correct_tier1_count = 0` over the 38 point cases → `gate_false_escalation = null` **by definition** (no correct Tier-1 to test the judge against) → the finder A/B + G3 sweep are **moot**. **Verified real, not a harness artifact** (expected_spans load, Scout runs/returns citations, oracle correct): Scout Tier-1 ≈ **0 correct** on SWE-bench point cases (right-file-wrong-span at best, e.g. astropy `separable.py:66-102` vs gold `242-248`). **Not a model-swap fix** — `qwen3:4b-instruct` as Scout also 0/3, **identical wrong span** on astropy → span-level localization / task difficulty, not model quality. **Env findings:** `qwen3-coder:30b` Deep+judge **OOM'd** the 32 GB host (swap 94% full — the D9 co-residency risk realized, an AC4/D7 `BLOCKED`-hold cause), resolved with `qwen3:8b`; and `make_instruct_judge` does **not** disable model "thinking" (anchored `_parse_score` would fail on `<think>`) — a latent 0018 SUT robustness gap, not biting `qwen3:8b`. **Named follow-up:** *Scout Tier-1 span-level localization on SWE-bench* — **not** gate-accuracy / threshold-calibration / finder-model-swap. **Honest scope:** mechanism + instrument shipped and unit-verified; G0/G1 validated live; **OQ2 gate calibration remains blocked upstream** (Scout locate accuracy) — the DEFERRED null IS the deliverable. **Deviations:** T7–T16 batched as one RED (full `test_oq2_protocol.py`) → one GREEN; T18 refactor a no-op; T19 harness built + G0/G1 live, full sweep operator-gated + moot; AC12 "skip-not-fail ≠ close" honored (closed on a recorded SUT-observing outcome).
- 2026-07-02 — **Spec 0019 (oq2-rerun) shipped the OQ2 re-run INSTRUMENT + gate-confound MECHANISM** (specs/0019-oq2-rerun/): a pure **measurement/eval** spec — SUT (tiers/gate/matrix/judge) **FROZEN**, all code in `harpyja/eval/`. Baseline **757 → 777 unit pass** (+20), ruff clean. **(1)** *A recommender refuses to calibrate over an instrument it has measured to be broken* — new `EvalConfig.gate_false_escalation_ceiling = 0.20` (eval-only, `Settings`-disjoint, **provisional**) + `recommend_oq2` dispatcher: a measured instruct false-escalation **strictly `> ceiling`** emits the `gate-confounded` typed null (`OUTCOME_GATE_CONFOUNDED`, carrying the rate) instead of tuning `verify_threshold` over a judge that rejects correct citations; `== ceiling`/`None` defer to the **unchanged** `rank_sweep`. Additive `Recommendation.outcome`/`.gate_false_escalation_measured`. **(2)** *Preflight is a setup-time doctor, single-seam + honesty-scoped* (D4) — `preflight_models_present` asserts `gateway.assert_local` FIRST (no second outbound path), then a deduped required-tag membership check naming the first absent tag; claims only **"pulled"**, NOT co-resident-loadable (OOM named as a G1-caught residual risk). +`cmd_preflight`/`preflight` subparser. **(3)** Report schema **0013/1 → 0014/1** (additive last-with-defaults: ceiling + gate-confound + instruct/scout A/B twins, hoisted to one `_GATE_CONFOUND_AGG_FIELDS` anti-drift source; legacy blocks still validate). **(4)** One-oracle characterization lock (both gate denominators flip with `_any_primary_overlap`). **(5)** Honesty: **mechanism + instrument shipped ≠ OQ2 calibrated** — the actual OQ2 recommendation/typed-null over the real N=12 subset needs the **operator** sweep (`HARPYJA_N12_FIXTURES` + served models); G1/G2/G3-at-scale are skip-not-fail demonstrations; **OQ2 not calibrated, B2 not closed, astropy-12907 not proven here**. Live preflight **PASSED** on this host. **Deviation (recorded):** `plan.md` scoped T11 scaffolding-only, but leaving `run_swebench_sweep` on `rank_sweep` left `gate-confounded` **wired-but-dormant** → `recommend_oq2` + ceiling wired INTO the runner (best-achievable instruct false-esc = min over measured grid points, instruct-base only), TDD'd (3 tests) within the frozen-SUT boundary. Follow-ups: the operator OQ2 sweep; permanent ceiling calibration; astropy-12907 end-to-end; per-span abstain; permanent `lm_model`; Q8 footprint/co-residency; Wave-2.1 substring/fuzzy.
- 2026-07-01 — **Spec 0018 (judge) shipped — the B2 fix from 0015** (specs/0018-judge/): closed **B2** (0015 `live-run-findings.md` D2) as a gate-quality / judging-**mechanism** fix — no tier/calibration/escalation/classifier/citation-format change. The Verification Gate reused `scout_model` (a FastContext citation-*finder* fine-tune) as a 0–1 relevance *scorer* and `_parse_score` grabbed the first number anywhere + clamped, so it false-rejected **correct** citations (astropy-12907 → `gate-low-confidence`). Six durable points. **(1)** *The judge must be in-distribution for the task* — new `make_instruct_judge` scores via `settings.lm_model` (served Qwen3-8B instruct, the 0016 flip made it available); OOD finder judge **retained non-default** (D5) as the OQ2 A/B baseline. **(2)** *A best-effort scorer degrades, never fabricates* — strict `_parse_score → float | None` (anchored, range-checked); a line number (`219`, the exact regression — must NOT clamp to `1.0`), out-of-range (`1.2`/`-0.1`), or prose (`0, because…`) → `None` → typed `ScoreParseError` → the *existing* `except → failed=True` fires (the 0017 "make the un-raisable raisable" move). Elegant unifier: out-of-range number and line number are one rule. Shared `_score_or_raise` → both judges degrade identically (AC13). **(3)** *A subclass degrade-cause is isinstance-checked FIRST* — `ScoreParseError ⊂ ValueError`, so `verify` tests it before the 0017 timeout branch and the generic branch; **exactly one** distinct WARNING, generic "scoring failed" asserted **absent** (AC7, on the record message not `caplog.text`). **(4)** `verify_method` becomes a **real selector** — `_VERIFY_METHODS = {scout_model, instruct_model}`, default flips to `instruct_model`; `select_judge`/`_JUDGE_FACTORIES` in `gate.py`, `build_verification_gate` dispatches. D7: whole-gate degrade on one non-conforming reply. **(5)** Stated coupling — `lm_model` now dual-consumer (Deep **and** the gate judge); provisional (0016 "for now"). **(6)** Honesty: **mechanism fixed ≠ accuracy proven** — astropy-12907 passing end-to-end needs a calibrated `verify_threshold` over the new score distribution = the OQ2 re-run; AC10/AC11 disclaimed as plumbing, changelog says "B2 *mechanism* fixed," never "B2 closed." **757 unit pass** (+32), ruff clean; T17 live instruct-judge smoke **passed live**. B1(0016)+B2(0018)+B3(0017) all fixed → **OQ2 re-run unblocked** (fresh spec). Follow-ups: per-span abstain, deterministic lexical judge, constrained-decoding parse, permanent `lm_model`, Q8 footprint, Wave-2.1 substring/fuzzy.
