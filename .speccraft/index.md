# Harpyja

A read-only, offline MCP server that turns a natural-language query into exact `file:line` citations across large/legacy/air-gapped codebases.

## Stack

- Python 3.12+
- FastMCP (MCP server; stdio + streamable HTTP)
- Tree-sitter (symbol layer: Go, Rust, Python, JS/TS, C#, Java, C/C++) + ripgrep fallback
- Tier 1 "Scout": native explorer loop (a general OpenAI-compatible tool-calling model over four read-only tools `{grep,glob,read_span,ls}`) — as of spec 0024 (+`ls` spec 0027); FastContext is FULLY REMOVED as of spec 0025 (no external finder dependency; the explorer is the sole Scout backend, running on `lm_model`)
- DSPy `dspy.RLM` over Deno/Pyodide WASM sandbox (Tier 2 "Deep")
- Local OpenAI-compatible model endpoint: llama.cpp (`llama-server`) or Ollama. As of spec 0025 the default `lm_model` (`hf.co/Qwen/Qwen3-8B-GGUF:latest`) serves BOTH the Scout explorer loop and Deep; any OpenAI-compatible tool-calling model is swappable. ⚠️ **8 GB footprint NOT validated**: an 8B-class model co-loaded with Deep + the Deno/Pyodide sandbox under `mode=auto` exceeds a small GPU — treat "8 GB" as aspirational, not a validated minimum
- `uv` for env/deps

## Architecture in one paragraph

A FastMCP server exposes three tools (`locate` / `read` / `index`) and holds no business logic. The Orchestrator classifies each query, then runs the cheapest tier that can answer: Tier 0 (deterministic tree-sitter symbols + ripgrep), Tier 1 (Scout, a native tool-calling explorer loop), and Tier 2 (Deep, a `dspy.RLM` over bounded read-only host tools). A Verification Gate reads cited lines back and escalates on failure; a Citation Formatter merges, ranks, and clamps results. The Model Gateway is the single outbound abstraction, pointed only at localhost — the one place the air-gap guarantee is enforced. Every layer has a fallback so retrieval never goes blind.

## Hard rules (see guardrails.md)

- No network egress at runtime — model/search/parsing stay local; only the Model Gateway calls out, and only to localhost.
- Read-only — Harpyja returns citations and never edits, writes, or generates code in the target repo.
- Always degrade gracefully — no parser → ripgrep; no model → Tier 0; escalation fails → best-effort Tier 1 with a confidence flag.

## Where to look

- MCP server / transports: `harpyja/server/`
- Routing, classifier, verification gate, formatter: `harpyja/orchestrator/`
- File walker / manifest / hashing: `harpyja/index/`
- Tree-sitter + ripgrep engines (`CodeSpan`): `harpyja/symbols/`
- Tier 1 Scout (native explorer loop; FastContext fully removed as of spec 0025): `harpyja/scout/`
- Tier 2 Deep (`dspy.RLM` + host tools): `harpyja/deep/`
- Model Gateway (llama.cpp / Ollama): `harpyja/gateway/`
- Settings / profiles: `harpyja/config/`
- CLI entrypoints (serve/index/locate/read): `harpyja/cli.py`

## Active spec

none

## Recent decisions (last 3)

- 2026-07-08 — **Spec 0030 (symbols) SHIPPED the 5th explorer tool — a Tier-0 file-local `symbols(path)` wrapper (no new parser; kind+span CodeSpans, path-normalized, confined-after-resolution, clamped by new `Settings.scout_symbols_max_entries=400`; manifest-`degraded` → ripgrep fallback with visible `degraded: true` marker). Exact-tool-count convention amended 4 → 5 IN LOCKSTEP. Wiring threads `symbol_records` + `manifest`. Shared `record_to_codespan` projection extracted. Lift-report schema shipped (version-stamped 0030/1). TOOL PROVEN (20+ unit tests) and HARNESS VALIDATED with 5th tool present (clean run to terminal, no degrade). BUT THE LIFT HYPOTHESIS IS NOT MEASURED — recorded inconclusive-and-inconsistent, explicitly NOT "validated": the live bucket oracle is a `has_citations→CORRECT` proxy (not ground-truth span comparison), the astropy control moved to "CORRECT" via a mechanism a file-local tool cannot produce (internal inconsistency, false-capability read), symbols invocation was unconfirmed, and N=2 on an ad-hoc rig. The tool ships proven; the AC5/AC6 measurement is a HOLD naming a real lift run with ground-truth oracle as follow-up.** (specs/0030-symbols/)
- 2026-07-07 — **Spec 0028 (generation-control) BOUNDED the explorer's per-call generation and made truncation DIAGNOSABLE — but AC5 localization HELD AGAIN on a NEW downstream defect (parallel-tool-call echo mismatch)** (specs/0028-generation-control/): 0027 left AC5 a HOLD because generation was UNBOUNDED (masked as `model-unreachable` @300s; `model-unreachable ≠ can't-localize`). This spec bounds it and makes any residual runaway diagnosable. **Shipped (units 1046 pass, ruff clean; AC4 PROVEN LIVE):** (AC0) `ModelGateway.complete_with_tools` surfaces `finish_reason` additively from `choices[0].finish_reason` (the CHOICE, not the message; `str`-cast, `"unknown"` sentinel when absent) — the load-bearing enabler, a downstream degrade can't be TYPED without it; (AC2) `explorer_max_tokens=2048` cap, DRIFT-GUARD on the explorer-owned object (`ExplorerBackend.max_tokens=2048`, field-default introspection test), `ModelGateway` stays param-only so Deep is never capped; (AC1) `explorer_enable_thinking` knob via `chat_template_kwargs={"enable_thinking":false}` (NOT the inferior template-perturbing `/no_think` token), shipped provisional `True`; (AC3) the FIFTH explorer cause `scout-degraded:generation-truncated` — `finish=length` ⇒ truncation REGARDLESS of a riding valid tool_call, plumbed loop→backend→runner→report, `SCHEMA_VERSION 0027/1 → 0028/1`; (AC8) Deep-scope enforced by `explorer_`-prefixed field names + a `deep/test_rlm.py` outbound-field guard that rots false on leak; (AC4) first explorer call `finish=tool_calls` ~2s live (the 0027 "never finishes turn 1" symptom GONE). **AC5 HOLD RE-POINTED:** the 0027 unbounded-generation blocker is FIXED; a NEW distinct downstream defect diagnosed — the explorer loop echoes N parallel `tool_calls` (measured N=4) but answers only `tool_calls[0]`, leaving N−1 unanswered → malformed conversation → turn-2 runaway the cap correctly catches as `generation-truncated` (FULL echo → finish=length 101s; TRIMMED to answered → finish=tool_calls 0.8s). `generation-truncated ≠ can't-localize`. `test_harness_live.py` AC5 stays xfail, reason re-pointed to this mechanism. **Honest status:** the FOURTH consecutive genuine downstream layer (0026 under-powered instrument → 0027 eager-map + unbounded-generation → 0028 parallel-tool-call handling); AC5 (drive-to-citation) has been a recorded HOLD across 0027 → 0028, each by a different correctly-attributed cause — generation control PROVEN, drive-to-citation STILL UNPROVEN. **Named BLOCKING follow-up:** make the loop's assistant echo well-formed — OPEN design question (A) trim-to-answered [minimal, spends turns re-proposing N−1] vs (B) answer-all-N [OpenAI-correct, uses parallelism, larger 0024-loop blast radius], turn-budget tradeoff the crux — the prerequisite for AC5, the 0026 pilot re-run, and the model bake-off.
- 2026-07-07 — **Spec 0027 (harness) removed the explorer's EAGER whole-repo context map (RCA-driven push → pull), added an on-demand `ls` tool (exact-tool-count convention reconciled 3 → 4), and plumbed the four-cause degrade taxonomy per-cause to the report (`SCHEMA_VERSION 0026/1 → 0027/1`); cutover-not-redesign (gate/matrix/orchestrator/`submit_citations`/`Locator` byte-untouched)** (specs/0027-harness/): the 0026 RCA found `build_context_map` (~10,181-token whole-repo listing re-sent every turn) pushed the 16B into a generation that never completed turn 1 → degrade misread as non-localization. **Fix PROVEN live:** minimal `build_initial_prompt(query)` replaces the eager map (turn-1 payload ~10,181 → ~60 tokens, ~170× cut, repo-size-independent); `ls(path='.')` single-dir tool (`confine_path` + `scout_ls_max_entries`) is the layout-discovery affordance `glob` lacks; per-cause `scout_degrade_*_count` fields make a re-emptied case diagnosable (discriminant = typed cause/outcome, NEVER `turns_used`). **DEVIATION:** cutover is a minimal prompt, not literal `context_map=''` (the query was baked into the map). **AC5 HOLD — capability UNMEASURED:** both astropy+django degraded `model-unreachable` @300s on a DOWNSTREAM generation-runaway (Qwen3 thinking + unbounded generation; `/no_think`+cap → 13.2s, `/no_think` alone → 180s runaway), NOT the map defect and NOT can't-localize (`model-unreachable ≠ can't-localize`; the AC5 test ships `xfail`→xpass on fix). **AC7 committed-error correction:** 0020–0023 'confounded' (shipped in 1ef917f) narrowed to 0026-ONLY and ASSERTED consistent across all 3 surfaces (0fdcb57) — 0020–0023 ran on the retired FastContext backend (pre-0024), never touched the eager map. **Blocking follow-up:** generation control (thinking-off + tuned `max_tokens` + directive prompt) is a PREREQUISITE for the 0026 re-run, the bake-off, and ANY localization measurement — the harness is proven cheap-prompt, but model-drive-to-citation is UNPROVEN pending it. Units T1–T11 green; report `SCHEMA_VERSION 0027/1`.
- 2026-07-06 — **Spec 0026 (eval) built the terse-query ranking INSTRUMENT (not yet the SET): reuse the sha256-pinned patch-derived spans as labels JOINED by `case_id` (never transcribed), rewrite only the QUERY to Harpyja's terse shape; measurement/dataset harness, SUT byte-frozen** (specs/0026-eval/): 0020–0023 proved SWE-bench issue-prose the wrong query distribution; this builds the smallest paired RELATIVE-ranking instrument for the model bake-off (unblocks it, is NOT it). **JOIN-not-copy (AC1):** `terse_dataset.load_terse_dataset` asserts `sha256(raw.jsonl)==provenance` FIRST then joins `expected_spans`/`base_commit`/source-issue by `case_id` from the pinned `raw.jsonl` (sole authority; `base_commit` a raw-record key via `JoinMeta`, review B2; label provenance `patch-derived-at-convert`). **Version-gated loud loader (AC3/AC5):** NEW `DATASET_SCHEMA_VERSION="0026/1"` gates `_parse_case` — a terse row MAY omit spans but MUST carry the guard provenance, a legacy/seed row loads unchanged with defaults (additive-defaults + reject-if-missing coexist). **Leakage guard = EXECUTABLE two-model blind protocol (AC2):** loud-validated `authoring_provenance.py` sidecar (`AuthoringRecord`, verdict/outcome enums, hash-consistency, aggregate leaky/dropped) + operational pin-2 `assert_author_input_blind`; `terse_authoring.py` is an OFFLINE injected-seam tool (NOT the product `ModelGateway`, ast-guarded non-product). Labelled 'executable + reviewable', NOT 'structurally enforced'; STATE- not CAPABILITY-independence; model-CIRCULARITY named-not-closed; paired-ranking retained CO-PRIMARY as the model-independent floor. **AC8 pilot power-gate:** frozen/hashed `PREREGISTERED_AC8_CONFIG` (Qwen3-8B vs qwen3:4b-instruct, pilot 10→target 30, `min_discordant_pairs=8` reused) + signal-bearing flip excluding empty↔wrong-file noise; total `decide_ac8`→`UNDER_POWERED_STOP` below floor. Report `0025/1→0026/1` (pinned `representativeness_caveat`, Python monoculture named). **State: INFRA COMPLETE, instrument NOT yet usable — committed terse fixture is PLACEHOLDERS; the offline blind-authoring pilot (real queries) + live AC8 run are the delegated deliverable; a likely AC8 STOP is a SCOPED FINDING ('these two general candidates under-power terse-query ranking'), not a rerun-until-it-passes loop.** Suite 1010 pass, ruff clean. **Deviation:** T19 sha256-hoist deferred (would couple the light loader to heavy `swebench_eval`); one-oracle reuse pin done. Follow-ups: offline pilot; model bake-off; finder-capability `N38_PLUS_FINDER_CAPABILITY`; Axis-2 (legacy + non-Python evals); OQ2 threshold; Tier-0 symbol tool.
