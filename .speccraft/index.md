# Harpyja

A read-only, offline MCP server that turns a natural-language query into exact `file:line` citations across large/legacy/air-gapped codebases.

## Stack

- Python 3.12+
- FastMCP (MCP server; stdio + streamable HTTP)
- Tree-sitter (symbol layer: Go, Rust, Python, JS/TS, C#, Java, C/C++) + ripgrep fallback
- Tier 1 "Scout": native explorer loop (a general OpenAI-compatible tool-calling model over three read-only tools) — as of spec 0024; the retired Microsoft FastContext adapter remains in-tree off the production path, pending a cleanup spec
- DSPy `dspy.RLM` over Deno/Pyodide WASM sandbox (Tier 2 "Deep")
- Local OpenAI-compatible model endpoint: llama.cpp (`llama-server`) or Ollama (4B-class quantized, 8 GB GPU profile — ⚠️ **8 GB / Q4 floor NOT validated**: the recommended Q4_K_M Scout model is non-functional on real repos; the working config is Q8 (~2× memory, OOMs auto on 16 GB) — re-characterize the floor, see ARCHITECTURE.md "Footprint")
- `uv` for env/deps

## Architecture in one paragraph

A FastMCP server exposes three tools (`locate` / `read` / `index`) and holds no business logic. The Orchestrator classifies each query, then runs the cheapest tier that can answer: Tier 0 (deterministic tree-sitter symbols + ripgrep), Tier 1 (Scout, a native tool-calling explorer loop), and Tier 2 (Deep, a `dspy.RLM` over bounded read-only host tools). A Verification Gate reads cited lines back and escalates on failure; a Citation Formatter merges, ranks, and clamps results. The Model Gateway is the single outbound abstraction, pointed only at localhost — the one place the air-gap guarantee is enforced. Every layer has a fallback so retrieval never goes blind.

## Hard rules (see guardrails.md)

- No network egress at runtime — model/search/parsing stay local; only the Model Gateway calls out, and only to localhost.
- Read-only — Harpyja returns citations and never edits, writes, or generates code in the target repo.
- Always degrade gracefully — no parser → ripgrep; no model → Tier 0; escalation fails → best-effort Tier 1 with a confidence flag.

## Where to look

- MCP server / transports: `harpyja/server/`
- Routing, classifier, verification gate, formatter: `harpyja/orchestrator/`
- File walker / manifest / hashing: `harpyja/index/`
- Tree-sitter + ripgrep engines (`CodeSpan`): `harpyja/symbols/`
- Tier 1 Scout (native explorer loop; FastContext adapter retired off-path): `harpyja/scout/`
- Tier 2 Deep (`dspy.RLM` + host tools): `harpyja/deep/`
- Model Gateway (llama.cpp / Ollama): `harpyja/gateway/`
- Settings / profiles: `harpyja/config/`
- CLI entrypoints (serve/index/locate/read): `harpyja/cli.py`

## Active spec

none

## Recent decisions (last 3)

- 2026-07-06 — **Spec 0024-v2 (Scout v2) RETIRED the FastContext Tier-1 backend and REPLACED it with a self-contained native `ExplorerBackend` behind the UNCHANGED `ScoutBackend` seam — a general OpenAI-compatible tool-calling model driven over EXACTLY three read-only tools (grep/glob/read_span) to a tool-call-native `submit_citations` terminal action; unit-complete AND live-green** (specs/0024-v2/): the FastContext 4B was retracted/unobtainable (only broken quants) — unshippable regardless of retrieval quality; owning the loop also removes the buggy third-party harness confound, yielding a clean model-AGNOSTIC rig for the later bake-off. **Shipped (all behind the byte-unchanged `ScoutBackend`/`ScoutEngine`/`Locator` seam):** `explorer_tools.build_explorer_tools` (EXACTLY `{grep,glob,read_span}`, each `confine_path`-guarded + Settings-bounded + read-only, mirroring `deep/host_tools`; `grep`/`glob` share the SAME `RipgrepEngine` as Deep's `search` — one bounded rg source of truth; `glob`→file-level CodeSpans); `context_map.build_context_map` (pre-model filtered tree from the manifest, NO file bytes; vendor/test/generated exclusion is DISPLAY-only, tool scope unaffected — map-filter ≠ tool-scope filter); `submit.submit_citations` + `SubmitCitationsSchemaError` (**OQ2 resolved** — tool-call-native STRICT-schema terminal action REPLACES the 0011/0012 `<final_answer>` text grammar; a diagnosis-shaped field failing schema is the ENFORCEABLE locator-not-diagnoser guard; normalized via unchanged `normalize_spans` to `source_tier=1`); `explorer_loop.run_explorer_loop` (one call/turn, `scout_max_turns` cap + distinct whole-loop `scout_wall_clock_s` ceiling; loop-detection on exact `(tool_name, normalized_args)` no-new-span repeat; **citation-preserving truncation — AC5 proves the negative: a find older than the bloat threshold STILL resolves post-truncation via a re-injected dropped-span index, never converted to honest-empty**); `explorer_backend.ExplorerBackend` (`assert_local()` before the loop starts — air-gap before any I/O; four distinct `ScoutUnavailable` causes `model-unreachable`/`loop-turns-exhausted`/`loop-wallclock-exhausted`/`backend-error` → Tier-0 floor; honest-empty submit ≠ degrade; first-class `degrade_rate`); new `ModelGateway.complete_with_tools`; five provisional `Settings` budgets (`scout_max_turns=12` etc., flagged for the bake-off — OQ1/OQ3). **Result:** all 10 ACs met; both `@pytest.mark.integration` PASS LIVE (Qwen3-8B on loopback Ollama, ~28s, zero non-loopback egress); full suite 1015 pass / 23 skip, ruff clean. **Deviation:** shipped as a PARALLEL production factory `build_explorer_scout_engine` (NOT an in-place swap of `build_scout_engine`); the FastContext factory + eval callers left byte-untouched so the backend swap and the deferred FastContext deletion do not entangle in one diff. **Staged follow-ups (out of scope):** AST symbol tool (Tier-0-as-a-tool), the model bake-off (OQ1/OQ3 budget tuning), the representative eval set, and the FastContext code+dependency deletion (closes the parallel-factory deviation).
- 2026-07-05 — **Spec 0023 OPERATOR RUN — fired the reformulation probe on REAL SWE-bench long-issue text (FastContext-4B, no instrument change): formal `HOLD_INCONCLUSIVE`, but QUERY_SHAPE FALSIFIED + 0022 `RETRIEVAL_FUNDAMENTAL` corroborated → next spec is FINDER-CAPABILITY** (specs/.archive/0023-benchmark-fit/operator-run-findings.md): the operator measurement 0023 named as follow-up #1; runs the byte-frozen instrument via public seams only (no `harpyja/` change), `HARPYJA_REQUIRE_LIVE_STACK=1`, air-gap held. SUT = **FastContext-1.0-4B-RL-Q8** (`scout_model`); qwen3:8b is only the labeled LLM sensitivity distiller. **Precondition gate PASSED** (real case → `is_raw_issue` admits, usable_n=1 not 0, both arms run, McNemar cell increments — fires on real input unlike terse fixtures). **Primary (mechanical) arm, stopped at pre-registered floors** (usable_n=14, discordant=8; remaining cases 5–30 min each): raw `EMPTY×11, RIGHT_FILE_WRONG_SPAN×2, WRONG_FILE×1` (**79% empty, 0/14 span-CORRECT**), `delta_empty=+0.143` (< 0.20 band), `delta_file_accuracy=−0.071`, McNemar p=0.727 (no reject) → **Axis-1 = INCONCLUSIVE(AXIS_SIGNAL_DISAGREEMENT)** (floors MET → substantive, not power). **LLM arm** (7/14 accepted, guard hard-rejects 7; scored n=5, `llm_delta_empty=−0.20`): the identifier-RETAINING smart distiller ALSO fails → identifier-stripping wasn't the confound (closes the "rule too crude → QUERY_SHAPE" escape). **Axis-2** `representative=True` by rule (doc_density=high; noted limitation — the low∧weak gate under-detects a documented weak-proxy like SWE-bench). **Composed 2×2 → `HOLD_INCONCLUSIVE`.** Substantive: **QUERY_SHAPE FALSIFIED** (terser queries don't cut emptiness or improve right-file across both distillers), 0022's `RETRIEVAL_FUNDAMENTAL` corroborated on REAL long-issue text; the formal gate can't certify CAPABILITY only because the near-zero localization floor makes discordant pairs empty↔wrong-file noise — itself evidence the bottleneck is retrieval capability. **Next spec = FINDER-CAPABILITY (`N38_PLUS_FINDER_CAPABILITY`), NOT a reformulation/query layer, NOT a benchmark swap.** Ops lessons: decouple Scout-arm vs LLM-distiller phases (single-GPU model-swap thrash); the operator callable must apply the pre-registered `LLM_PROMPT` (guarded distiller passes raw issue_text).
- 2026-07-05 — **Spec 0023 (benchmark-fit) shipped the typed two-axis PRE-REGISTERED discriminator (query-shape McNemar probe × representativeness record) that decides whether 0022's `RETRIEVAL_FUNDAMENTAL` is a capability wall or a `BENCHMARK_UNREPRESENTATIVE` artifact — SUT byte-frozen** (specs/0023-benchmark-fit/): additive under `harpyja/eval/`. **+52 unit** (`test_benchmark_fit.py` ×30, `test_distill.py` ×12, `test_locate_probe.py` +10), green, ruff clean; 7 Scout-only integration PASS LIVE (~56s). **Shipped:** `benchmark_fit.py` (exact two-sided McNemar from `math.comb`, no scipy; frozen `PREREGISTERED_CONFIG` `MIN_DISCORDANT_PAIRS=8`/`DELTA_EMPTY_BAND=0.20`/`min_n=12`; `PairedRow`+`aggregate_paired` within-case deltas + discordant `(b,c)` FROM pairs; total `decide_axis1` w/ paired uncertainty gate + three named `INCONCLUSIVE` triggers; `RepresentativenessRecord`+`is_representative`; total `compose_verdict` pre-registered 2×2 where Axis 2 downgrades Axis 1 routing — the cell names the next spec); `distill.py` (DUAL distiller — `mechanical_distill` PRIMARY, structurally blind by token-subset extraction + code-identifier strip, gold-blind, `MECHANICAL_RULE_HASH`; `llm_distill_guarded` LABELED non-deciding sensitivity arm w/ post-hoc subset hard-reject, `LLM_PROMPT_HASH`); `locate_probe.py` EXTENDED (AC7, fields last-with-defaults — 0022 byte-compatible; `run_paired_reformulation_probe`; `is_raw_issue`). **Honest scope:** live smoke non-informative BY CONSTRUCTION (terse legacy fixtures → `usable_n=0`, all in `excluded_case_ids` — the AC8 self-flag working) — the operator VERDICT is deliberately NOT emitted; firing it needs real SWE-bench long-issue cases (≥12 usable, ≥8 discordant) under `HARPYJA_REQUIRE_LIVE_STACK=1`, so 0022's `RETRIEVAL_FUNDAMENTAL` stands and `BENCHMARK_UNREPRESENTATIVE` is not-yet-excluded. **Honest cost:** a binary paired probe is not "a handful" — power lives in discordant pairs, ~15–25 raw cases to reach 8. **Follow-ups:** the operator run (fire the discriminator; a `QUERY_SHAPE`/¬representative outcome routes to a benchmark/query-layer spec, NOT a finder swap); OQ1 (reachability floor 8 → power floor ~12–15); OQ2 (promote `delta_file_accuracy` into the primary rule?).
