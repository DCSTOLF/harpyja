# Harpyja

A read-only, offline MCP server that turns a natural-language query into exact `file:line` citations across large/legacy/air-gapped codebases.

## Stack

- Python 3.12+
- FastMCP (MCP server; stdio + streamable HTTP)
- Tree-sitter (symbol layer: Go, Rust, Python, JS/TS, C#, Java, C/C++) + ripgrep fallback
- Tier 1 "Scout": native explorer loop (a general OpenAI-compatible tool-calling model over three read-only tools) — as of spec 0024; FastContext is FULLY REMOVED as of spec 0025 (no external finder dependency; the explorer is the sole Scout backend, running on `lm_model`)
- DSPy `dspy.RLM` over Deno/Pyodide WASM sandbox (Tier 2 "Deep")
- Local OpenAI-compatible model endpoint: llama.cpp (`llama-server`) or Ollama. As of spec 0025 the default `lm_model` (`hf.co/Qwen/Qwen3-8B-GGUF:latest`) serves BOTH the Scout explorer loop and Deep; any OpenAI-compatible tool-calling model is swappable. ⚠️ **8 GB footprint NOT validated**: an 8B-class model co-loaded with Deep + the Deno/Pyodide sandbox under `mode=auto` exceeds a small GPU — treat "8 GB" as aspirational, not a validated minimum
- `uv` for env/deps

## Architecture in one paragraph

A FastMCP server exposes three tools (`locate` / `read` / `index`) and holds no business logic. The Orchestrator classifies each query, then runs the cheapest tier that can answer: Tier 0 (deterministic tree-sitter symbols + ripgrep), Tier 1 (Scout, a native tool-calling explorer loop), and Tier 2 (Deep, a `dspy.RLM` over bounded read-only host tools). A Verification Gate reads cited lines back and escalates on failure; a Citation Formatter merges, ranks, and clamps results. The Model Gateway is the single outbound abstraction, pointed only at localhost — the one place the air-gap guarantee is enforced. Every layer has a fallback so retrieval never goes blind.

## Hard rules (see guardrails.md)

- No network egress at runtime — model/search/parsing stay local; only the Model Gateway calls out, and only to localhost.
- Read-only — Harpyja returns citations and never edits, writes, or generates code in the target repo.
- Always degrade gracefully — no parser → ripgrep; no model → Tier 0; escalation fails → best-effort Tier 1 with a confidence flag.

## Where to look

- MCP server / transports: `harpyja/server/`
- Routing, classifier, verification gate, formatter: `harpyja/orchestrator/`
- File walker / manifest / hashing: `harpyja/index/`
- Tree-sitter + ripgrep engines (`CodeSpan`): `harpyja/symbols/`
- Tier 1 Scout (native explorer loop; FastContext fully removed as of spec 0025): `harpyja/scout/`
- Tier 2 Deep (`dspy.RLM` + host tools): `harpyja/deep/`
- Model Gateway (llama.cpp / Ollama): `harpyja/gateway/`
- Settings / profiles: `harpyja/config/`
- CLI entrypoints (serve/index/locate/read): `harpyja/cli.py`

## Active spec

none

## Recent decisions (last 3)

- 2026-07-06 — **Spec 0026 (eval) built the terse-query ranking INSTRUMENT (not yet the SET): reuse the sha256-pinned patch-derived spans as labels JOINED by `case_id` (never transcribed), rewrite only the QUERY to Harpyja's terse shape; measurement/dataset harness, SUT byte-frozen** (specs/0026-eval/): 0020–0023 proved SWE-bench issue-prose the wrong query distribution; this builds the smallest paired RELATIVE-ranking instrument for the model bake-off (unblocks it, is NOT it). **JOIN-not-copy (AC1):** `terse_dataset.load_terse_dataset` asserts `sha256(raw.jsonl)==provenance` FIRST then joins `expected_spans`/`base_commit`/source-issue by `case_id` from the pinned `raw.jsonl` (sole authority; `base_commit` a raw-record key via `JoinMeta`, review B2; label provenance `patch-derived-at-convert`). **Version-gated loud loader (AC3/AC5):** NEW `DATASET_SCHEMA_VERSION="0026/1"` gates `_parse_case` — a terse row MAY omit spans but MUST carry the guard provenance, a legacy/seed row loads unchanged with defaults (additive-defaults + reject-if-missing coexist). **Leakage guard = EXECUTABLE two-model blind protocol (AC2):** loud-validated `authoring_provenance.py` sidecar (`AuthoringRecord`, verdict/outcome enums, hash-consistency, aggregate leaky/dropped) + operational pin-2 `assert_author_input_blind`; `terse_authoring.py` is an OFFLINE injected-seam tool (NOT the product `ModelGateway`, ast-guarded non-product). Labelled 'executable + reviewable', NOT 'structurally enforced'; STATE- not CAPABILITY-independence; model-CIRCULARITY named-not-closed; paired-ranking retained CO-PRIMARY as the model-independent floor. **AC8 pilot power-gate:** frozen/hashed `PREREGISTERED_AC8_CONFIG` (Qwen3-8B vs qwen3:4b-instruct, pilot 10→target 30, `min_discordant_pairs=8` reused) + signal-bearing flip excluding empty↔wrong-file noise; total `decide_ac8`→`UNDER_POWERED_STOP` below floor. Report `0025/1→0026/1` (pinned `representativeness_caveat`, Python monoculture named). **State: INFRA COMPLETE, instrument NOT yet usable — committed terse fixture is PLACEHOLDERS; the offline blind-authoring pilot (real queries) + live AC8 run are the delegated deliverable; a likely AC8 STOP is a SCOPED FINDING ('these two general candidates under-power terse-query ranking'), not a rerun-until-it-passes loop.** Suite 1010 pass, ruff clean. **Deviation:** T19 sha256-hoist deferred (would couple the light loader to heavy `swebench_eval`); one-oracle reuse pin done. Follow-ups: offline pilot; model bake-off; finder-capability `N38_PLUS_FINDER_CAPABILITY`; Axis-2 (legacy + non-Python evals); OQ2 threshold; Tier-0 symbol tool.
- 2026-07-06 — **Spec 0025 (removal) DELETED the FastContext surface + dependency and made the native `ExplorerBackend` the SOLE Scout backend** (specs/0025-removal/): closed the parallel-factory deviation 0024 left open — FastContext's upstream 4B was retracted/unobtainable, and two Scout paths is two things to keep honest, and the bake-off must measure through the real backend. **Shipped (staged deletion, not a redesign — the explorer loop / `ScoutBackend` seam / gate / matrix / orchestrator untouched):** `build_scout_engine` is now the SINGLE canonical factory over `ExplorerBackend` (`build_explorer_scout_engine` deleted); its default gateway PINS `settings.lm_model` (not `ModelGateway.model`'s `"local"`, which 404s on Ollama's tag-routed API), so the explorer runs on `lm_model` (Qwen3-8B — same as Deep). **Turns-used MIGRATED before the seam was cut:** `LoopResult.turns_used` → `ExplorerBackend.last_turns_used` (submit + both exhaustion paths, reset per run) → `ScoutEngine.last_turns_used`; the 0022 diagnostic repointed (`turns_used_source` now `"explorer"`) BEFORE `agent_factory=` / `count_turns` / `counting_agent_factory` were deleted. **`normalize.py` DISENTANGLED:** only the FC-era suffix-recovery (`_recover_suffix`/`MIN_TAIL_SEGMENTS`, 0012) removed; the shared `normalize_spans_with_tally`/`ScoutTally`/`last_tally` core KEPT (feeds runner/locate_probe/0022 locate_accuracy), recovered counts → structurally zero. **Deleted:** `fastcontext.py`, `client.py` (+ the 0007 env-injection apparatus + the 0011 `<final_answer>` grammar), `tools.py` (a SECOND-ORDER orphan), FC error causes + FC-only Settings fields, the `fastcontext` git dep (pyproject + uv.lock). `scout_model` KEPT as the served gate A/B baseline (scoped OUT of removal). **Report schema `0014/1 → 0025/1`** (recovery fields retired-to-zero; the kept `fc_citation_*` shape-tally fields now populated by the explorer — a `fc_`-prefix naming debt recorded as backend-neutral). **Two behavior changes rode in:** the isolated eval probe now FLOORS a typed `ScoutUnavailable` to EMPTY (not a crash — the explorer RAISES on exhaustion where FastContext returned honest-empty); and the `lm_model` pin is a production-correctness fix (Scout would have 404'd on Ollama). Executable absence guards `test_fastcontext_absent.py` + `test_packaging.py` rot-false on reintroduction. **Result:** suite **985 pass / 23 skip**, ruff clean; AC8 cutover LIVE-green (Qwen3-8B on loopback Ollama, zero non-loopback egress). Follow-ups (unchanged): representative eval set; model bake-off (OQ1/OQ3); Tier-0 symbol tool; finder-capability work (`N38_PLUS_FINDER_CAPABILITY`).
- 2026-07-06 — **Spec 0024-v2 (Scout v2) RETIRED the FastContext Tier-1 backend and REPLACED it with a self-contained native `ExplorerBackend` behind the UNCHANGED `ScoutBackend` seam — a general OpenAI-compatible tool-calling model driven over EXACTLY three read-only tools (grep/glob/read_span) to a tool-call-native `submit_citations` terminal action; unit-complete AND live-green** (specs/0024-v2/): the FastContext 4B was retracted/unobtainable (only broken quants) — unshippable regardless of retrieval quality; owning the loop also removes the buggy third-party harness confound, yielding a clean model-AGNOSTIC rig for the later bake-off. **Shipped (all behind the byte-unchanged `ScoutBackend`/`ScoutEngine`/`Locator` seam):** `explorer_tools.build_explorer_tools` (EXACTLY `{grep,glob,read_span}`, each `confine_path`-guarded + Settings-bounded + read-only, mirroring `deep/host_tools`; `grep`/`glob` share the SAME `RipgrepEngine` as Deep's `search` — one bounded rg source of truth; `glob`→file-level CodeSpans); `context_map.build_context_map` (pre-model filtered tree from the manifest, NO file bytes; vendor/test/generated exclusion is DISPLAY-only, tool scope unaffected — map-filter ≠ tool-scope filter); `submit.submit_citations` + `SubmitCitationsSchemaError` (**OQ2 resolved** — tool-call-native STRICT-schema terminal action REPLACES the 0011/0012 `<final_answer>` text grammar; a diagnosis-shaped field failing schema is the ENFORCEABLE locator-not-diagnoser guard; normalized via unchanged `normalize_spans` to `source_tier=1`); `explorer_loop.run_explorer_loop` (one call/turn, `scout_max_turns` cap + distinct whole-loop `scout_wall_clock_s` ceiling; loop-detection on exact `(tool_name, normalized_args)` no-new-span repeat; **citation-preserving truncation — AC5 proves the negative: a find older than the bloat threshold STILL resolves post-truncation via a re-injected dropped-span index, never converted to honest-empty**); `explorer_backend.ExplorerBackend` (`assert_local()` before the loop starts — air-gap before any I/O; four distinct `ScoutUnavailable` causes `model-unreachable`/`loop-turns-exhausted`/`loop-wallclock-exhausted`/`backend-error` → Tier-0 floor; honest-empty submit ≠ degrade; first-class `degrade_rate`); new `ModelGateway.complete_with_tools`; five provisional `Settings` budgets (`scout_max_turns=12` etc., flagged for the bake-off — OQ1/OQ3). **Result:** all 10 ACs met; both `@pytest.mark.integration` PASS LIVE (Qwen3-8B on loopback Ollama, ~28s, zero non-loopback egress); full suite 1015 pass / 23 skip, ruff clean. **Deviation:** shipped as a PARALLEL production factory `build_explorer_scout_engine` (NOT an in-place swap of `build_scout_engine`); the FastContext factory + eval callers left byte-untouched so the backend swap and the deferred FastContext deletion do not entangle in one diff. **Staged follow-ups (out of scope):** AST symbol tool (Tier-0-as-a-tool), the model bake-off (OQ1/OQ3 budget tuning), the representative eval set, and the FastContext code+dependency deletion (closes the parallel-factory deviation).
