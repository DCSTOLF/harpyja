# Registration domain

Consolidated, current requirements for MCP client registration recipes. Each
line carries its originating spec(s) as provenance.

- Verified registration recipes exist for `.mcp.json` (Claude Code) and `config.toml` (Codex). (spec 0001)
